- content_for :content do

  article.staff.split-view
    nav.split-view-sidebar
      ul.split-view-nav
        li.style-guide class=(active_nav('style_guide'))
          = link_to(t('.style_guide'), staff_style_guide_path)
        li.switch-view class=(active_nav('mobile_desktop_preferences'))
          = link_to(t('.mobile_desktop_preferences'), staff_mobile_desktop_preference_path)
        li.background-jobs
          = link_to(t('.background_jobs'), staff_background_jobs_path)
    .staff-content.split-view-main
      = yield

= render template: 'layouts/application'
