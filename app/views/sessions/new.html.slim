= simple_form_for @user, url: sessions_path do |f|
  = f.input :username, input_html: { autofocus: true }
  = f.input :password
  = f.input :remember_me, as: :boolean, label: false, \
    inline_label: 'Yes, remember me'
  = f.button :submit, 'Sign in'
p Don't have an account? #{link_to 'Sign up', sign_up_path} for one now!
