= form_tag(search_path, method: :get, class: "search form-search") do
  = search_field_tag :q, search_query.q, placeholder: t('.placeholder'), class: 'query'
  = submit_tag t('.button'), name: nil, class: 'search-button'
