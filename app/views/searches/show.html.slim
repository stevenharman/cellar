article.search-results
  .search-results-header
    == render 'searches/form', search_query: search_query, autofocus: true

  .search-results-summary= t('searches.show.summary', count: number_with_delimiter(@search_result.total_count))
  ul.search-results-list
    - for result in @search_result do
      == render partial: result

  == paginate @search_result
